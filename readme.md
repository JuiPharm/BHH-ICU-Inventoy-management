# ICU Stock Management System

ระบบจัดการคลังยาและเวชภัณฑ์ ICU แบบครบวงจร ประกอบด้วย Frontend บน GitHub Pages และ Backend บน Google Apps Script

## 📋 สารบัญ

- [คุณสมบัติ](#คุณสมบัติ)
- [โครงสร้างระบบ](#โครงสร้างระบบ)
- [ข้อกำหนดเบื้องต้น](#ข้อกำหนดเบื้องต้น)
- [การติดตั้ง](#การติดตั้ง)
  - [1. ตั้งค่า Google Apps Script](#1-ตั้งค่า-google-apps-script)
  - [2. ตั้งค่า GitHub Pages](#2-ตั้งค่า-github-pages)
  - [3. การเชื่อมต่อ Frontend และ Backend](#3-การเชื่อมต่อ-frontend-และ-backend)
- [การใช้งาน](#การใช้งาน)
- [การแก้ไขปัญหา](#การแก้ไขปัญหา)
- [คำแนะนำด้านความปลอดภัย](#คำแนะนำด้านความปลอดภัย)

## ✨ คุณสมบัติ

### ฟังก์ชันหลัก
- ✅ จัดการคลังสินค้า (เพิ่ม, แก้ไข, ลบ)
- ✅ ตรวจเช็คสินค้าประจำวัน (แยกตามบทบาท RN/PN)
- ✅ บันทึกการเบิกใช้สินค้า
- ✅ ระบบแจ้งเตือนสินค้าขาดและหมดอายุ
- ✅ รายงานและสถิติการใช้งาน
- ✅ สำรองข้อมูลอัตโนมัติ

### ระบบควบคุม
- 🔐 ระบบ Login แบบ Role-Based (Admin, RN, PN)
- 📝 บันทึก Audit Log ทุกการกระทำ
- 🔒 ใช้ LockService ป้องกันการแก้ไขพร้อมกัน
- ⚡ ระบบ CORS สำหรับ GitHub Pages
- 📧 ระบบส่งอีเมลแจ้งเตือนอัตโนมัติ

## 🏗️ โครงสร้างระบบ

```
ICU Stock Management System
├── Google Apps Script (Backend)
│   ├── Code.gs          # API และ Business Logic
│   └── appsscript.json  # Configuration
│
├── GitHub Pages (Frontend)
│   ├── index.html       # Main HTML
│   ├── app.js          # JavaScript Application
│   ├── styles.css      # Styles
│   └── README.md       # This file
│
└── Google Sheets (Database)
    ├── Inventory
    ├── Daily Check Supply
    ├── Daily Check Medicine
    ├── Usage Logs
    ├── Expired Items
    ├── Reorder Items
    ├── Staff
    ├── Email Recipients
    ├── Audit Logs
    └── Settings
```

## 📋 ข้อกำหนดเบื้องต้น

1. **Google Account** - สำหรับ Google Apps Script และ Google Sheets
2. **GitHub Account** - สำหรับ GitHub Pages
3. **Google Sheets** - สำหรับเก็บข้อมูล
4. **ความรู้พื้นฐาน** - HTML, CSS, JavaScript (ไม่จำเป็นต้องเชี่ยวชาญ)

## 🚀 การติดตั้ง

### 1. ตั้งค่า Google Apps Script

#### ขั้นตอนที่ 1: สร้าง Google Sheets
1. ไปที่ [Google Sheets](https://sheets.google.com)
2. สร้าง Spreadsheet ใหม่
3. บันทึก Spreadsheet ID (จะใช้ในขั้นตอนถัดไป)
   - Spreadsheet ID คือสตริงยาวใน URL: `https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit`

#### ขั้นตอนที่ 2: สร้าง Google Apps Script
1. ใน Google Sheets ไปที่ **Extensions > Apps Script**
2. ลบไฟล์ Code.gs ที่มีอยู่ทั้งหมด
3. สร้างไฟล์ใหม่ชื่อ **Code.gs** และวางโค้ดจากไฟล์ `Code.gs` ในโปรเจกต์นี้
4. แก้ไขบรรทัดที่ 15:
   ```javascript
   const SPREADSHEET_ID = "YOUR_SPREADSHEET_ID_HERE";
   ```
   แทนที่ด้วย Spreadsheet ID ของคุณ

5. สร้างไฟล์ **appsscript.json** และวางโค้ดจากไฟล์ `appsscript.json`

#### ขั้นตอนที่ 3: ตั้งค่า Services
1. คลิกที่ **Services** (รูปเฟือง) ในแถบด้านซ้าย
2. เพิ่ม Services ต่อไปนี้:
   - Google Sheets API
   - Google Drive API

#### ขั้นตอนที่ 4: Deploy Web App
1. คลิกที่ **Deploy > New deployment**
2. เลือกประเภท **Web app**
3. ตั้งค่าดังนี้:
   - **Description**: ICU Stock Management API
   - **Execute as**: Me
   - **Who has access**: Anyone
4. คลิก **Deploy**
5. บันทึก Web App URL (จะใช้ในขั้นตอนถัดไป)
   - URL จะมีรูปแบบ: `https://script.google.com/macros/s/{SCRIPT_ID}/exec`

#### ขั้นตอนที่ 5: Initialize Database
1. เปิด Web App URL ในเบราว์เซอร์
2. ใช้ Postman หรือ cURL เรียก API เพื่อสร้างชีต:
   ```bash
   curl -X POST "YOUR_WEB_APP_URL" \
        -H "Content-Type: text/plain;charset=utf-8" \
        -d '{"action":"initializeSheets"}'
   ```
3. หรือสร้างไฟล์ HTML ชั่วคราวเพื่อทดสอบ:
   ```html
   <script>
   fetch('YOUR_WEB_APP_URL', {
       method: 'POST',
       headers: {'Content-Type': 'text/plain;charset=utf-8'},
       body: JSON.stringify({action: 'initializeSheets'})
   }).then(r => r.json()).then(console.log);
   </script>
   ```

### 2. ตั้งค่า GitHub Pages

#### ขั้นตอนที่ 1: สร้าง Repository
1. ไปที่ [GitHub](https://github.com)
2. สร้าง Repository ใหม่ (สาธารณะหรือส่วนตัว)
3. ตั้งชื่อ เช่น `icu-stock-management`

#### ขั้นตอนที่ 2: อัพโหลดไฟล์
1. อัพโหลดไฟล์ต่อไปนี้ลงใน Repository:
   - `index.html`
   - `app.js`
   - `styles.css`
   - `README.md`

2. สามารถทำได้หลายวิธี:
   - อัพโหลดผ่าน GitHub UI
   - ใช้ Git CLI
   - ใช้ GitHub Desktop

#### ขั้นตอนที่ 3: เปิดใช้งาน GitHub Pages
1. ใน Repository ของคุณ ไปที่ **Settings**
2. เลื่อนลงไปที่ **Pages** (ในเมนูด้านซ้าย)
3. เลือก **Source**: Deploy from a branch
4. เลือก **Branch**: main (หรือ master)
5. เลือก **Folder**: / (root)
6. คลิก **Save**
7. รอสักครู่ GitHub จะสร้างเว็บไซต์ให้
8. เว็บไซต์จะอยู่ที่: `https://{your-username}.github.io/{repository-name}`

### 3. การเชื่อมต่อ Frontend และ Backend

#### ขั้นตอนสำคัญ: แก้ไข API URL
1. เปิดไฟล์ `app.js` ใน GitHub Repository
2. แก้ไขบรรทัดที่ 8:
   ```javascript
   API_BASE_URL: "<PUT_WEB_APP_EXEC_URL_HERE>"
   ```
   แทนที่ด้วย Google Apps Script Web App URL ของคุณ
   
   ตัวอย่าง:
   ```javascript
   API_BASE_URL: "https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxxxxx/exec"
   ```

3. Commit และ Push การเปลี่ยนแปลง
4. รอ GitHub Pages rebuild (ประมาณ 1-2 นาที)

## 📱 การใช้งาน

### การเข้าสู่ระบบครั้งแรก

1. ไปที่ GitHub Pages URL ของคุณ
2. ระบบจะขอให้เข้าสู่ระบบ
3. ต้องเพิ่มผู้ใช้งานครั้งแรกผ่าน Google Apps Script:

   **วิธีที่ 1: ใช้ Apps Script Console**
   1. ไปที่ Apps Script ของคุณ
   2. รันฟังก์ชัน `addStaff` โดยตรง:
   ```javascript
   function testAddStaff() {
       const result = addStaff({
           staffId: "admin001",
           name: "ผู้ดูแลระบบ",
           password: "admin123",
           role: "Admin"
       }, "SYSTEM", "Admin");
       console.log(result);
   }
   ```

   **วิธีที่ 2: ใช้ API โดยตรง**
   ```bash
   curl -X POST "YOUR_WEB_APP_URL" \
        -H "Content-Type: text/plain;charset=utf-8" \
        -d '{"action":"addStaff","payload":{"staffData":{"staffId":"admin001","name":"ผู้ดูแลระบบ","password":"admin123","role":"Admin"},"staffId":"SYSTEM","role":"Admin"}}'
   ```

### บทบาทผู้ใช้งาน

- **Admin**: ทำได้ทุกอย่าง
- **RN (Registered Nurse)**: ตรวจเช็คยา (Medicine) เท่านั้น
- **PN (Practical Nurse)**: ตรวจเช็คเวชภัณฑ์ (Medical Supply) เท่านั้น

### ฟังก์ชันหลัก

1. **คลังสินค้า**: เพิ่ม/แก้ไข/ลบ รายการสินค้า
2. **ตรวจเช็ค**: บันทึกการตรวจสอบประจำวัน
3. **สั่งซื้อ**: แสดงรายการที่ต้องสั่งซื้อ
4. **หมดอายุ**: แสดงรายการหมดอายุ
5. **การเบิกใช้**: บันทึกและดูประวัติการเบิก
6. **รายงาน**: สรุปต่างๆ
7. **ตั้งค่า**: สำหรับ Admin เท่านั้น

## 🔧 การแก้ไขปัญหา

### ปัญหาทั่วไป

#### 1. "ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์ได้"

**สาเหตุ**: API URL ไม่ถูกต้องหรือ CORS ไม่ทำงาน

**วิธีแก้:**
- ตรวจสอบว่า API_BASE_URL ใน `app.js` ถูกต้อง
- ตรวจสอบว่า Web App ถูก deploy แล้ว
- ตรวจสอบใน Console ของเบราว์เซอร์ (F12)

#### 2. "เข้าสู่ระบบไม่สำเร็จ"

**สาเหตุ**: ยังไม่มีผู้ใช้งานในระบบ

**วิธีแก้:**
- เพิ่มผู้ใช้งานครั้งแรกผ่าน Apps Script (ดูวิธีในส่วนการใช้งาน)

#### 3. "ไม่มีข้อมูลในตาราง"

**สาเหตุ**: ยังไม่ได้สร้างชีตหรือไม่มีข้อมูล

**วิธีแก้:**
- เรียก API `initializeSheets` (ดูในส่วนการตั้งค่า)
- เพิ่มข้อมูลด้วยมือใน Google Sheets

#### 4. GitHub Pages ไม่แสดงผล

**สาเหตุ**: ยังไม่ได้เปิดใช้งานหรือยังไม่ได้ build

**วิธีแก้:**
- รอสักครู่ (GitHub Pages ใช้เวลา build)
- ตรวจสอบใน Settings > Pages ว่าแสดงสีเขียวแล้วหรือไม่
- ตรวจสอบว่าไฟล์ HTML อยู่ใน root directory

### การ Debug

1. **เปิด Console ของเบราว์เซอร์** (F12)
2. **ดู Network Tab** - ตรวจสอบ API calls
3. **ดู Console Tab** - ดู error messages
4. **ตรวจสอบ Google Apps Script Logs** - View > Logs

## 🔒 คำแนะนำด้านความปลอดภัย

### สำหรับ Production

1. **เปลี่ยนรหัสผ่านเริ่มต้น** ทันทีที่เข้าสู่ระบบ
2. **ใช้ HTTPS เท่านั้น** - GitHub Pages และ Google Apps Script รองรับ
3. **จำกัดการเข้าถึง Web App** - ตั้งค่า "Who has access" เป็น "Only myself" หรือ "Anyone within organization"
4. **ตั้งค่าแจ้งเตือนอีเมล** เพื่อรับแจ้งเตือนการเปลี่ยนแปลง
5. **สำรองข้อมูลบ่อยๆ** ใช้ฟังก์ชัน backupData

### การปรับปรุงความปลอดภัย (Optional)

1. **เพิ่ม Authentication Token**:
   - ใช้ JWT หรือ Session Token
   - เก็บใน sessionStorage หรือ cookie

2. **เข้ารหัสรหัสผ่าน**:
   - ใช้ bcrypt หรือ SHA-256
   - ไม่เก็บรหัสผ่านแบบ plain text

3. **Rate Limiting**:
   - จำกัดจำนวนการเรียก API
   - ใช้ CacheService ของ Google Apps Script

4. **Input Validation**:
   - เพิ่มการตรวจสอบข้อมูลเข้า
   - ป้องกัน SQL Injection (ถึงแม้จะใช้ Google Sheets)

### การจัดการข้อผิดพลาด

ระบบมี Error Handling อยู่แล้ว แต่สามารถปรับปรุงเพิ่ม:

1. **เพิ่ม Try-Catch มากขึ้น**
2. **Log ข้อผิดพลาดละเอียดขึ้น**
3. **แจ้งเตือน Admin เมื่อเกิด Error**

## 📞 การขอความช่วยเหลือ

หากพบปัญหา:

1. **ตรวจสอบส่วน "การแก้ไขปัญหา"** ก่อน
2. **ตรวจสอบ Console Logs** ของเบราว์เซอร์
3. **ตรวจสอบ Google Apps Script Logs**
4. **สร้าง Issue ใน GitHub** (ถ้ามี)

## 📄 ใบอนุญาต

โปรเจกต์นี้เป็น Open Source สามารถใช้งานและแก้ไขได้ตามต้องการ

## 🙏 ขอบคุณ

ขอบคุณที่ใช้งาน ICU Stock Management System

หากมีคำถามหรือข้อเสนอแนะ สามารถติดต่อได้ตามช่องทางที่ระบุไว้ในระบบ
